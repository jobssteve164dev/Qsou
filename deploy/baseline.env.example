# 对外端口
QSOU_API_PORT=8000
QSOU_WEB_PORT=3000

# 前端构建时写入浏览器的 API 地址。远程部署时必须改成用户浏览器可访问的地址。
QSOU_PUBLIC_API_URL=http://localhost:8000/api/v1

# JSON 数组；填写允许访问 API 的前端来源。
QSOU_CORS_ORIGINS=["http://localhost:3000"]

# 按需运行的采集器
QSOU_SPIDER=financial_news
